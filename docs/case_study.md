# Synthetic Case Study

The synthetic fixture describes scored cases with uncertainty widths. A top-mean policy selects the highest predicted means under a fixed action budget. An uncertainty-robust policy subtracts an uncertainty penalty before selecting the same number of cases.

The project is not claiming that the robust formula is the correct policy for any real-world setting. It is a stress test showing that action-set selection can depend on uncertainty treatment.
